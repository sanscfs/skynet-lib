[project]
name = "skynet-scoring"
version = "2026.4.4"
description = "Skynet memory scoring: carrying-capacity decay, PageRank, logical-time decay, consolidation thresholds"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
