[project]
name = "skynet-graph"
version = "2026.4.7"
description = "Skynet memory graph: similarity edges, traversal, co-occurrence accumulation, cluster detection"
requires-python = ">=3.12"
dependencies = []

[project.optional-dependencies]
louvain = ["python-louvain>=0.16"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
