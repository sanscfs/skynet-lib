[project]
name = "skynet-embedding"
version = "2026.4.21"
description = "Skynet embedding pipeline: Ollama -> OpenRouter -> hash fallback"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.27",
    "skynet-core>=2026.4.2",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
