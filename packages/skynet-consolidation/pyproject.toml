[project]
name = "skynet-consolidation"
version = "2026.4.11"
description = "Skynet memory consolidation: merge mature cliques into summary points with provenance"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
