[project]
name = "skynet-matrix"
version = "2026.4.1"
description = "Skynet Matrix client, stream events, and live streaming"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.27",
    "skynet-core>=2026.4",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
