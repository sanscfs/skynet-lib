[project]
name = "skynet-qdrant"
version = "2026.4.3"
description = "Skynet Qdrant vector DB client -- sync & async, httpx-based"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.27",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
